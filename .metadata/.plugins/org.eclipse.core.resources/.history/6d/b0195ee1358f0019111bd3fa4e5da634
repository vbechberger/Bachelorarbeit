package genetic;

public class Vertex {
	int number;
	
	Vertex(int number) {
		setNumber(number);
	}
	
	
	
	void setNumber(int number) {
		if (number <= 0) {
			throw new IllegalArgumentException("The number of "
					+ "the city has to be a positive integer!");
		}
		this.number = number;
	}


}
