package genetic;

public class Edge {
	
	private int startVertex = -1;
	private int endVertex = -1;

	private double value = -1;
	

	Edge(int startVertex, int endVertex, double value) {
		setValue(value);
	}

	void setStartVertex(int startVertex) {
		if (value <= 0) {
			throw new IllegalArgumentException(
					"The distance " + "							between cities has to be positive!");
		}
		this.value = value;
	}
	
	void setValue(double value) {
		if (value <= 0) {
			throw new IllegalArgumentException(
					"The distance " + "							between cities has to be positive!");
		}
		this.value = value;
	}

}
